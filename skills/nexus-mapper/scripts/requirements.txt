# nexus-mapper scripts dependencies
# For standalone use or distribution installation
# When used within the Nexus project, dependencies are already covered by the poetry environment (tree-sitter ^0.25.2 + tree-sitter-language-pack)

tree-sitter>=0.22.0
tree-sitter-language-pack
